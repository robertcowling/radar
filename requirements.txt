boto3>=1.35.0
botocore>=1.35.0
requests>=2.32.0
Pillow>=10.4.0
cairosvg>=2.7.0
numpy>=1.26.0
h5py>=3.11.0
pyproj>=3.6.0
openai>=1.0.0
pandas>=2.2.0
shapely>=2.0.0
# Storms pipeline (fetch_storms.py): google-cloud-storage is the only GCS
# writer in this repo, needed because storms.csv lives in the sibling
# floodforecast project's GCS bucket, not R2 — see CLAUDE.md. pypdf reads the
# Met Office storm report PDFs that feed the LLM summary.
google-cloud-storage>=2.10.0
pypdf>=4.0.0
