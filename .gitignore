venv/
__pycache__/
*.pyc
*.h5
.claude/
.env
diag.py
accum_24hr.png
.wrangler/
data_mslp/
data_aifs_mslp/
static/mslp/
static/aifs_mslp/
warnings/archive/
rfg/archive/

# FGS archive: fetch_fgs.py local mirror; canonical copy lives on R2
fgs/
static/radar5/
static/radar5_gh/
data_h5_opera/
data_h5/
status/history.json
catalog.json

europe_geo.json
uk_regions_r2.geojson
radarmicro/composites/*.webp

# EA flood warnings: large/derived files live on R2, not git
ea/floodareas/polygons/
ea/floodareas/polygons_all.min.geojson
ea/counts/
ea/timeline/

# NRW flood warnings: same pattern as EA above
nrw/floodareas/polygons/
nrw/floodareas/polygons_all.min.geojson
nrw/counts/
nrw/timeline/

# Raw DataMapWales source exports for build_nrw_floodareas.py — 7-19MB input
# data, re-downloaded occasionally, not a repo asset.
/nrw_source/

# Raw BNG source for reproject_rainfall_summary.py — superseded by the
# committed WGS84 output (rainfall_summary_areas.geojson).
/rainfallsummarypolygon.json

# Surface obs: raw downloads / local build output / manifest live on R2 or
# ephemeral CI workspace, not git
surface_obs/raw/
surface_obs/out/
surface_obs/manifest.json

# OS Open UPRN source CSV for count_properties.py — ~2GB, downloaded fresh
# each time it's needed and deleted afterward, never committed
osopenuprn_*.csv
osopenuprn_*.zip

# Locally-rebuilt flood_history/* output (area_stats.json etc.) — staged here
# before upload_flood_history.py pushes it to R2; large/derived, not git.
/flood_history_build/
